export class Tarefa{
    id?: string;
    descricao?: string;
    todo?: boolean;
    doing?: boolean;
    done?: boolean;

    // constructor(id?: string, tarefa: Tarefa = {}) {
    //   this.id = id;
    //   this.descricao = tarefa.descricao;
    // }
   
}